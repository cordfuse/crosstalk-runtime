FROM node:18-alpine
RUN apk add --no-cache git openssh-client
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY dist/ ./dist/
CMD ["node", "dist/runner.js", "--config", "/app/config.yaml"]
